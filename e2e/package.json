{
  "name": "apiplant-e2e",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "End-to-end tests: a real database, a real apiplant server, and the admin dashboard driven in a browser.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "shots": "pnpm shots:admin && pnpm shots:back-office && pnpm shots:impersonation && pnpm shots:ai && pnpm shots:studio && pnpm shots:cli && pnpm shots:optimize",
    "shots:optimize": "python3 ./scripts/optimize-shots.py",
    "shots:admin": "playwright test --config screenshots.config.ts shots/admin.shots.ts",
    "shots:back-office": "APP_DIR=examples/27-back-office APP_ORIGIN=http://127.0.0.1:8106 playwright test --config screenshots.config.ts shots/back-office.shots.ts",
    "shots:impersonation": "APP_DIR=examples/27-back-office APP_ORIGIN=http://127.0.0.1:8106 playwright test --config screenshots.config.ts shots/impersonation.shots.ts",
    "shots:ai": "APP_DIR=examples/19-ai APP_ORIGIN=http://127.0.0.1:8102 playwright test --config screenshots.config.ts shots/ai.shots.ts",
    "shots:studio": "playwright test --config studio-screenshots.config.ts",
    "shots:cli": "python3 ./scripts/cli-shots.py && node ./scripts/render-ansi.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@xterm/xterm": "^6.0.0"
  }
}
