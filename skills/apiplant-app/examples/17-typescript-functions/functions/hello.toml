# Per-deployment configuration for the `hello` function. The host converts this
# to JSON and hands it over via `ctx.config()`, the same value a C function would
# read through the `host.config` callback.
greeting = "Ciao"
