{
  "name": "slug",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "An apiplant function directory: TypeScript with npm dependencies.",
  "//": "`module` is where apiplant looks for the bundle; `build` is what it runs.",
  "module": "dist/slug.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --format=esm --platform=neutral --main-fields=module,main --external:apiplant --outfile=dist/slug.js"
  },
  "dependencies": {
    "slugify": "^1.6.6"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
