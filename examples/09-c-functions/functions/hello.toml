# Per-deployment configuration for the `hello` function. The host converts this
# to JSON and hands it over via `host->config(host->ctx)` — the C ABI's
# equivalent of `ctx.config()` in Rust.
greeting = "Ciao"
