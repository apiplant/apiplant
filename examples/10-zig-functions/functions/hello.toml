# Per-deployment configuration for the `hello` function. The host converts this
# to JSON and hands it over via `host.config`, the same C ABI callback a function
# written in C would use — the equivalent of `ctx.config()` in Rust.
greeting = "Ciao"
