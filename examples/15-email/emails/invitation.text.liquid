{% if inviter != '' %}{{ inviter }} would like you in {{ organization }}.{% else %}You have been asked to join {{ organization }}.{% endif %}
Accepting sets a password and puts you straight inside — there is nothing to
sign up for first.

Join {{ organization }}:
{{ url }}

The invitation stops working in {{ expires_in }}. If you weren't expecting it,
ignore this message — nothing happens until the link is used.
