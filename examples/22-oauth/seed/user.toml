# Sign in to the dashboard as admin@example.com, password `password`.
#
# `password` is not a field of the resource — it is hashed, with argon2, into
# whichever field the resource's `[auth]` section names. The plaintext never
# reaches the database, and a fixture stays readable.

[[row]]
id = "admin"
email = "admin@example.com"
password = "password"
display_name = "Ada Admin"
email_verified_at = 2024-01-08T09:00:00Z

[[row]]
id = "editor"
email = "editor@example.com"
password = "password"
display_name = "Eli Editor"
email_verified_at = 2024-01-09T11:30:00Z

[[row]]
id = "member"
email = "member@example.com"
password = "password"
display_name = "Mo Member"
email_verified_at = 2024-01-10T15:45:00Z
